<%= simple_form_for @nvt do |f| %>
  <%= f.input :name, placeholder: 'nom de votre NVT' %>
  <%= f.input :date, placeholder: 'date de creation' %>
  <%= f.input :price, placeholder: 'entrez votre prix %>
  <%= f.input :photo, as: :file %>
  <%= f.submit class: 'btn btn-flat' %>
<% end %>
